<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Favoriler - Soundsea</title>
    <link rel="stylesheet" href="/css/favorite.css">
    <link rel="stylesheet" href="/css/home.css">
</head>
<body>
    <!-- Navbar -->
    {{> navbar}}

    <div class="container">
        <h1>Favoriler</h1>
        
        <div class="tabs">
            <button class="tab-button" data-tab="audio">Sesli Kitaplar</button>
            <button class="tab-button active" data-tab="ebook">E-Kitaplar</button>
        </div>

        <div id="audio-favorites" class="tab-content" style="display: none;">
            {{#favorite_audiobooks}}
            <div class="book-card">
                <img src="{{cover_image}}" alt="{{title}}">
                <div class="book-info">
                    <h3>{{title}}</h3>
                    <p class="author">{{author}}</p>
                    <a href="/audio_book_detail/{{id}}" class="details-btn">Detayları Gör</a>
                </div>
            </div>
            {{/favorite_audiobooks}}
            {{^favorite_audiobooks}}
            <p class="no-books">Henüz favori sesli kitap bulunmuyor.</p>
            {{/favorite_audiobooks}}
        </div>

        <div id="ebook-favorites" class="tab-content">
            {{#favorite_books}}
            <div class="book-card">
                <img src="{{cover_image}}" alt="{{title}}">
                <div class="book-info">
                    <h3>{{title}}</h3>
                    <p class="author">{{author}}</p>
                    <a href="/ebook/{{id}}" class="details-btn">Detayları Gör</a>
                </div>
            </div>
            {{/favorite_books}}
        </div>
    </div>

  

    <script src="/js/favorites.js"></script>
      <script src="/js/navbar.js"></script>
</body>
</html>
