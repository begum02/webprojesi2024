<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{book.title}}</title>
    <!-- CSS Dosyasını Yükleme -->
    <link rel="stylesheet" href="/css/audio_book_detail.css">
</head>

<body>
    <!-- Sayfa Başlığı -->
    <div class="header">
        Sesli Kitap: {{book.title}}
    </div>

    <!-- Kitap Detayları -->
    <div class="book-container">
        <div class="book-details">
            <img src="{{book.cover_image}}" alt="{{book.title}}">
            <div class="book-content">
                <h1>{{book.title}}</h1>
                <h3>Yazar: {{book.author}}</h3>
                <p>{{book.description}}</p>
                <p><strong>Süre:</strong> {{book.duration}}</p>
                <audio controls>
                    <source src="{{book.audio_file}}" type="audio/mpeg">
                    Tarayıcınız bu sesi oynatmayı desteklemiyor.
                </audio>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <div class="footer">
        Tüm hakları saklıdır. | Kitap Platformu
    </div>
</body>

</html>
