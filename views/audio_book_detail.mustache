<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{book.title}}</title>
    <!-- CSS Dosyasını Yükleme -->
    <link rel="stylesheet" href="/css/audio_book_detail.css">
    <link rel="stylesheet" href="/css/navbar.css">
</head>

<body>
  <script src="/js/navbar.js"></script>
    
        {{> navbar}}
 
    <!-- Kitap Detayları -->
    <div class="book-container">
        <div class="book-details">
         <div class="book-image-rating">
            <img src="{{book.cover_image}}" alt="{{book.title}}">
            <div class="rating-favorite-container">
                <div class="audiobookrating" data-book-id="{{book.id}}" data-user-id="{{user.id}}">
                    <span class="star" data-value="1">&#9733;</span>
                    <span class="star" data-value="2">&#9733;</span>
                    <span class="star" data-value="3">&#9733;</span>
                    <span class="star" data-value="4">&#9733;</span>
                    <span class="star" data-value="5">&#9733;</span>
                </div>
                <button class="favorite-btn" data-book-id="{{book.id}}" data-is-favorite="{{isFavorite}}">
                    {{#isFavorite}}
                        Favorilerden Kaldır
                    {{/isFavorite}}
                    {{^isFavorite}}
                        Favorilere Ekle
                    {{/isFavorite}}
                </button>
            </div>
        </div>

            <div class="book-content">
                <h1>{{book.title}}</h1>
                <h3>Yazar: <span>{{book.author}}</span></h3>
                <h4>Kategori: <span>{{book.category}}</span></h4>
                <p><strong>Tanım: </strong>{{book.description}}</p>
                 <p><strong>Ortalama Puan: </strong><span id="audiobook-average-rating">{{averageRating}}</span></p>
               
                <audio controls>
                    <source src="{{book.audio_file}}" type="audio/mpeg">
                    Tarayıcınız bu sesi oynatmayı desteklemiyor.
                </audio>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <div class="footer">
        Tüm hakları saklıdır. | Kitap Platformu
    </div>
    <script src="/js/rating.js"></script>
    <script src="/js/audio_book_detail.js"></script>
</body>

</html>
